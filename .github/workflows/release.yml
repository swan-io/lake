name: Release

on:
  pull_request:
    types: [closed]

jobs:
  release:
    runs-on: ubuntu-latest
    name: Release
    if: ${{ github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release-v') }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: lts/*
          cache: yarn

      - name: Install dependencies
        run: yarn install --pure-lockfile

      - name: Typecheck
        run: yarn typecheck

      - name: Lint
        run: yarn lint

      - name: Run tests
        run: yarn test

      - name: Check licenses
        run: yarn license-check

      - name: Build
        run: yarn build

      - name: Get version
        id: version
        run: echo version=${{ github.event.pull_request.head.ref }} | sed -e 's/release-//g' >> $GITHUB_OUTPUT

      - name: Get changelog
        id: changelog
        shell: bash
        # trick for multiline variables
        run: |
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "${{ github.event.pull_request.body }}" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub release
        run: gh release create ${{ steps.version.outputs.version }} --title ${{ steps.version.outputs.version }} --notes ${{ steps.changelog.outputs.changelog }}

      # - name: Publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      #   run: |
      #     echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
      #     yarn workspace @swan-io/lake publish --tag latest
      #     yarn workspace @swan-io/shared-business publish --tag latest

      - name: Final check
        run: |
          echo ${{ steps.version.outputs.version }}
          echo ${{ steps.changelog.outputs.changelog }}

      # - name: Notify on Slack
      #   uses: bloodyowl/slack-message-release-action@v1.1.5
      #   with:
      #     version: ${{ steps.version.outputs.version }}
      #     changelog: ${{ steps.changelog.outputs.changelog }}
      #     slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
